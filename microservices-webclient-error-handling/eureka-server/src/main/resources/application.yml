server:
  port: 8761
spring:
  application:
    name: eureka-server # Default Eureka Server port
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
